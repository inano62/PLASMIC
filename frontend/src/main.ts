// // 例: frontend/src/main.ts から呼ぶ
// import { startLiveKit } from './livekit';
//
// // まず scripts/mint-token.js で作ったトークン文字列を貼る（検証用）
// const token = '<<ここに mint-token の出力を貼る>>';
// startLiveKit(token).catch(console.error);
